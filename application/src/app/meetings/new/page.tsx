import { Layout } from '@/components/layout';
import { NewMeetingPage } from '@/components/pages/new_meeting';

const Page = () => {
  return (
    <Layout>
      <NewMeetingPage />
    </Layout>
  );
};

export default Page;
